#include <string>
#include <iostream>
#include "..\bh_util.h"

struct boss{};
struct map{};

// FUNCTION PROTOTYPE
static void ignor(void);
static float tofu(int);
static double mpg(double);
static long summation(long [], int);
static double doctor(const std::string);
static void ofcourse(boss);
static std::string plot(const map *);

void ch7re2(void)
{
	std::cout << " - Chapter 7 Exercise 2 - " << std::endl << std::endl;

	std::cout << "See source code." << std::endl;

	std::cout << std::endl << "Press Enter to exit..." << std::endl;
}